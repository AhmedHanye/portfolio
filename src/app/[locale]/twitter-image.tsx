import OGImage, {
  alt,
  size,
  contentType,
  generateStaticParams,
} from "./opengraph-image";

// fallow-ignore-next-line unused-export
export { alt, size, contentType, generateStaticParams };

export default OGImage;
